## Example local server with traefik

* run setup_vhosts.sh
* docker-compose -d up // to start traefik
* docker-compose -f docker-compose.example_site.yml up // to create the demo sites to register into traefik